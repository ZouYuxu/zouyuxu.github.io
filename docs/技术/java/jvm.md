# 内存区域

![Java 运行时数据区域（JDK1.8 ）](https://oss.javaguide.cn/github/javaguide/java/jvm/java-runtime-data-areas-jdk1.8.png)

### 线程私有：

1. 程序计数器，记录当前执行的行数
2. 虚拟机栈：

调用方法会将栈帧压入栈中,调用完弹出栈帧

栈帧:由一下组成

1. 局部变量:
2. 操作数栈: 临时变量中间计算结果
3. 动态链接: 调用别的方法时,将class文件常量池里的指向方法的符号引用转为内存地址的直接引用
4. 返回地址

两种报错:stackOverFlow 循环导致的压入栈的深度超过最大深度

OutOfMemory,无法申请到足够多的空间

3. 本地方法栈，使用的native方法的栈

### 线程共享

#### 1. 堆

堆内存用来存放线程共享的对象

分为

1. 新生代：Eden、S0、S1（Survivor），对象首先在Eden区，在一次新生代垃圾回收后，对象存活会进入S0、S1，初始化为1，最大15，就进入老年代。
2. 老年代：
3. 永久代

最大值为啥是15，因为在对象头中记录年龄，一般是4位，最大可以表示十进制的15，

#### 2. 方法区
