
### redis过期事件实现延时任务缺点

#### 时效性差

过期事件是在Redis服务器删除key的时候通知，而不是过期的时候通知的

Redis服务器删除key的时候两种策略

1. 只有取出key的时候进行过期检查
2. 定期删除：每隔一段时间抽取一批key进行删除过期的key的操作。但是限制删除操作的时长和频率，避免影响到CPU的操作。因此可能超过了过期时间，还没被删除，无法发出过期事件
